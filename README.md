#include <stdio.h>
int main () {
int side;
printf("Enter side of squar: ");
scanf("%d", & side);
printf(" area is: "%d", side * side);
return 0;
}
#include <stdio.h>

int main() {
    float radius;
    printf("Enter radius: ");
    scanf("%f", &radius);
    printf("area is: %f", 3.14 * radius * radius);
    return 0;
}
